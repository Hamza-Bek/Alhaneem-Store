@page "/"
@using WebUI.Site.Components
@inject IJSRuntime JS

<HeroContent />

<About />

<ProductSlider SliderType="newest" />

<ProductSlider SliderType="oldest" />

<ServicesBox />

<LocationContent />

<FooterContent />

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JS.InvokeVoidAsync("initSwiperSliders");
        }
    }
}